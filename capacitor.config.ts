import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nm-devhomework-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
